<!DOCTYPE html>
<html>
  <div class="container">
    <head>
      <title id="logo">Blocipedia</title>
      <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
      <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
      <%= csrf_meta_tags %>
    </head>
    <body>
      <header class="navbar navbar-fixed-top navbar-inverse">
        <ul class="nav navbar-nav navbar-right">
          <li><%= link_to "Home", root_path %></li>
          <li><%= link_to "About", about_path %></li>
          <li><% if user_signed_in? %>
            <%= current_user.email %></li>
          <!-- turn current_user.email into a drop down with this <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> -->
          <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link' %></li>
          <% else %>
          <%= link_to "Sign In", new_user_session_path, :class => 'navbar-link' %>
          <% end %>
        </ul>
      </header>
          <div class="center jumbotron">
            <div class="container">
              <% if flash[:notice] %>
                <div class="alert alert-success">
                  <button type="button" class="close" data-dismiss="alert">&times;</button>
                  <%= flash[:notice] %>
                </div>
              <% elsif flash[:alert] %>
                <div class="alert alert-warning">
                  <button type="button" class="close" data-dismiss="alert">&times;</button>
                  <%= flash[:alert] %>
                </div>
              <% end %>
              <%= yield %>
            </div>
            <p class="notice"><%= notice %></p>
            <p class="alert"><%= alert %></p>
            <!-- Check if the user signed in. If so, display their email address w/unique identifier and button to sign out. -->
                <div><% if user_signed_in? == false %>
                    <!-- we display Sign up and Sign in buttons if the user is not logged in. -->
                  <%= link_to "Sign Up", new_user_registration_path, :class => 'navbar-link' %>
                  <% end %>
                </div>
          </div>

      </body>
      <footer>
        <h4>footer</h4>
      </footer>
  </div>
</html>
